# --- core ---
pydantic>=2.7
python-dotenv>=1.0
psutil>=5.9              # identifying the process behind a window

# --- Windows UI Automation ---
uiautomation>=2.0.20 ; sys_platform == "win32"
comtypes>=1.4 ; sys_platform == "win32"
pywin32>=306 ; sys_platform == "win32"

# --- screenshots / image handling ---
pillow>=10.0

# --- extraction backends (optional, see README) ---
anthropic>=0.40          # vision-LLM extraction (primary)
pytesseract>=0.3.10      # OCR fallback; needs the Tesseract binary on PATH
